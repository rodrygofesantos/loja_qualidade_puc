window.LabCharts={bars(elementId,labels,values){const root=document.getElementById(elementId);if(!root)return;root.innerHTML="";const max=Math.max(...values,1);labels.forEach((label,index)=>{const row=document.createElement("div");row.className="bar-row";const name=document.createElement("span");name.textContent=label;const track=document.createElement("div");track.className="bar-track";const fill=document.createElement("div");fill.className="bar-fill";fill.style.width=`${Math.max(2,values[index]/max*100)}%`;track.append(fill);const value=document.createElement("strong");value.textContent=values[index];row.append(name,track,value);root.append(row);});}};

